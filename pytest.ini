[pytest]
asyncio_mode = auto
addopts = -q
testpaths = tests
python_files = test_*.py

markers =
    unit: fast, isolated logic tests with no external I/O
    integration: tests involving Massive WS, NBBO, ContractEngine, or ChainAggregator
    shadow: full orchestrator dry-run tests with no trading or execution

